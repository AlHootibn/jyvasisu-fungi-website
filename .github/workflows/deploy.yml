name: Deploy to Netlify

on:
  push:
    branches: [master, main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Netlify deploy
        run: curl -X POST -d '{}' https://api.netlify.com/build_hooks/69fdfe8655901a168889d53a
